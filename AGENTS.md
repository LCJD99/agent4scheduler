# AGENTS

## Environment

- All development, test, and verification commands for this repository must use the `conda` environment named `agent`.
- Prefer `conda run -n agent <command>` for non-interactive commands.
- If an interactive shell is required, activate `agent` before running repository commands.
